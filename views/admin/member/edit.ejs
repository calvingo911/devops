<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>bbkal - Get Your Calculators Out, This Will Be Fun!</title>
    <link rel="stylesheet" href="https://www.unpkg.com/bulma@0.9.3/css/bulma.css">
</head>

<body>
    <div id="app" class="container is-max-desktop">
        <div class="field"><label class="label">Member Name</label>
            <div class="control"><input type="text" placeholder="Member Name" class="input" name="name" id="name"
                    value=<%= Member.name %> ></div>
        </div>
        <div class="field"><label class="label">Member Active</label>
            <div class="control"><input type="text" placeholder="Member active" class="input" name="price" id="active"
                    value=<%= Member.active %>></div>
        </div>
        <div class="field"><label class="label">Member Role</label>
            <div class="control"><input type="text" placeholder="admin / regular / special" class="input" name="role" id="role"
                    value=<%= Member.role %>></div>
        </div>
        <div class="control">
            <button class="button is-success" id="save" onclick=<%= "edit("+Member.id+")" %>> SAVE</button>
        </div>
    </div>
    <script src="https://unpkg.com/axios/dist/axios.min.js"></script>
    <script>
        const edit = (id) => {
            axios({
                method: 'put',
                url: '/admin/api/member/' + id,
                data: {
                    name: document.getElementById("name").value,
                    password: 'again default',
                    active: document.getElementById("active").value,
                    role: document.getElementById("role").value,
                }
            }).then((result) => {
                alert('Info status: ' + result.status)
                window.location = "/admin/member";
            }).catch((err) => {
                alert('error ' + err)
            });
        }
    </script>
</body>

</html>